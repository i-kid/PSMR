[running]
cpu = 16
;flag.StringVar(&mode, "m", "local", "on local or cluster")
net = local
runtimes = 10000
conflictRate = 0
keynum =100000
numClient = 1
numRequest = 1
batchsize = 1
logSpeed=1
mode =1
db=1
WTofile=0

;mode=? 0:early, 1:extreme and fast 2: none 
;db=? 0:redis, 1:go map


;flag.IntVar(&id, "i", 0, "The id of server,0,1,2,3")
;flag.StringVar(&mode, "m", "local", "on local or cluster")
;flag.IntVar(&applyId, "ai", 0, "0:apply mode,1:batchapply mode")
